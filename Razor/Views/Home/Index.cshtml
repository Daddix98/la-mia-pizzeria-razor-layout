@{
    ViewData["Title"] = "Home Page";
}

<!doctype html>
<html lang="en">
  <head>
    <!-- Required meta tags -->
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <title>Hello, world!</title>
  </head>
  <body>

      <div>
          <a>
              <strong>
                  1. Dichiarare una variabile valorizzandola con il nostro nome.
                     Dichiarare un'altra variabile valorizzandola con il nostro cognome.
                     Stampare il nostro nome e cognome all'interno di un tag h2.
              </strong>
          </a>

          <!--Soluzione punto 1-->

        @{
            string nome = "Davide";
        }

        <h2>@nome</h2>

        @{
            string cognome = "Scriboni";
        }

        <h2>@cognome</h2>

        <h2>@nome @cognome</h2>

        <a>
            <strong>
                2. Creare una funzione che stampa la somma dei numeri che vanno da 1 a n (con n passato come parametro - n incluso)
                   Stampare dentro uno span il risultato.
            </strong>
        </a>

        <!--Svolgimento punto 2-->
        <!--Creo una funzione che somma-->
        @{
            int sum(int n1, int n2)
            {
                return n1 + n2;
            }
        }
        <!--Utilizzo funzione somma-->

        <span><strong>@sum(14, 34)</strong></span>

        <p><strong>
            3. Contare quante volte la lettera a è presente nalla frase "nel mezzo del cammin di nostra vita"
            e stampare questo valore in pagina
       </strong> </p>

        <!--Punto 3-->
        @{
            string Frase = "nel mezzo del cammin di nostra vita";
            int conteggio = Frase.Split("a").Length - 1;
            <span>La lettera "a" si ripete  @conteggio volte </span>
        } 


        <p>
            <strong>
                4. Creare una List<int> di 20 numeri interi (generati random da 1 a 90, 1 e 90 inclusi)
                    e mostrare in pagina solo i numeri compresi tra 20 e 70
            </strong>
        </p>

      @{
            List<int> listaInteriRandom = new List<int>();

            Random random = new Random();

            for(int i = 0; i < 20; i++)
            {
                int numeroRandom = random.Next(1, 91);
                listaInteriRandom.Add(numeroRandom);
            }
            
            foreach(int numero in listaInteriRandom)
            {
                if(numero >=20 && numero <=70)
                {
                    <span>@numero</span>
                }  
            }

        }

        <p>
            <strong>
                5. Stampare in pagina la data e l'ora attuale (usando la classe DateTime) con il formato anno-mese-giorno ore:minuti:secondi
                6. Partendo dalla data di oggi (usando la classe DateTime) stampare in pagina
                a. la data di 7 giorni fa
                b. che giorno della settimana è oggi (il risultato dovrà essere il nome del giorno (Lunedì, Martedì, ...)
                c. che giorno della settimana sarà nello stesso giorno e mese di oggi ma tra un anno (il risultato dovrà essere il nome del giorno (Lunedì, Martedì, ...)
            </strong>

        </p>

        @{
            var oraAttuale = DateTime.Now;
            var dataPassata = oraAttuale - TimeSpan.FromDays(7);
            var giornoTraUnAnno = @oraAttuale.AddYears(1);
        }

        <span>@oraAttuale</span>
        <span>@oraAttuale.Year/ @oraAttuale.Month/ @oraAttuale.Day @oraAttuale.Hour @oraAttuale.Minute @oraAttuale.Second</span>

       <p> La data di 7 giorni fa è: @dataPassata</p>

       <p>Il giorno della settimana di oggi è:  @oraAttuale.ToString("ddd") </p>

       <p>Lo stesso giorno di oggi ma tra un anno è: @giornoTraUnAnno.ToString("ddd") </p>
           
        





   


      </div>
    

  

    <!-- Option 1: Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>

  </body>
</html>